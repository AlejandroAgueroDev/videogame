import React from "react";

const Bar=()=>{
  
 const handleSearch=()=>{

 }
  return (
    <div>
      <input type="text" placeholder="Buscar..." />
      <button onClick={handleSearch}>Buscar</button>
    </div>
  );
}

export default Bar;